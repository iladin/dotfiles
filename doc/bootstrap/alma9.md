# Bootstrap

## Install git and make

``` bash
sudo dnf install git make rsync
```

## Setup sudo

``` bash
make sudo
```

## Setup python

``` bash
python -m ensurepip
./requirements.txt
```

## Setup Mise

``` bash
makeMise.sh
```

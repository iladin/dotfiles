if [[ -f ~/.proxy ]]; then
		source ~/.proxy
fi
pathadd() {
    if [ -d "$1" ] && [[ ":$PATH:" != *":$1:"* ]]; then
        PATH="${PATH:+"$PATH:"}$1"
    fi
}

pathadd .
pathadd $HOME/bin
printenv
ls -al
test -d ~/.linuxbrew || makeLinuxBrew.sh
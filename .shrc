if [[ -f ~/.proxy ]]; then
		source ~/.proxy
fi
set -xv
pathadd() {
    if [ -d "$1" ] && [[ ":$PATH:" != *":$1:"* ]]; then
        PATH="${PATH:+"$PATH:"}$1"
    fi
}
chmod a+x bin/*
pathadd .
pathadd $HOME/bin
printenv
find .
test -d ~/.linuxbrew || source bin/makeLinuxBrew.sh
set +xv
if [[ -f ~/.proxy ]]; then
		source ~/.proxy
fi
set -xv
pathadd() {
    if [ -d "$1" ] && [[ ":$PATH:" != *":$1:"* ]]; then
        PATH="${PATH:+"$PATH:"}$1"
    fi
}

pathadd .
pathadd $HOME/bin
printenv
find .
test -d ~/.linuxbrew || makeLinuxBrew.sh